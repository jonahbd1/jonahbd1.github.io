# website-and-cv

Personal website and CV sources.

## Agent Runtime

For Python/runtime rules, follow `/Users/jbd/executive/docs/agent-runtime-policy.md`: do not run bare `python`, `python3`, `pip`, or `pytest` for project work; use `./scripts/python` or repo-local CLI wrappers.

Use `./scripts/python update_publications.py` for the publication updater.
